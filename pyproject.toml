[project]
name = "sf3"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "modal==1.5.6.dev27",
    "modal-training-gym",
]

[tool.uv]
# modal-training-gym pins modal==1.5.2; sessioned servers need a newer client.
override-dependencies = ["modal==1.5.6.dev27"]

[tool.uv.sources]
modal-training-gym = { git = "https://github.com/modal-projects/training-gym.git", branch = "main" }
